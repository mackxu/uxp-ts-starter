{
  "name": "uxp-ts-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build --watch --mode=dev",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "@types/photoshop": "^25.0.4",
    "patch-package": "^8.0.0",
    "typescript": "^5.2.2",
    "uxp-types": "^0.1.3",
    "vite": "^5.0.0"
  }
}
